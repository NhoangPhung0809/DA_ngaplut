.venv/
venv/
__pycache__/
*.zip
*.tar.gz
*.h5
*.pt
models/
pm/
*.pkl
*.deb
generate_documentation.py
# Chứa API key thật (OPENWEATHER_API_KEY, STORMGLASS_API_KEY, NOAA_CDO_TOKEN, ...) - KHÔNG được commit.
.env
TAI_LIEU_KY_THUAT_HE_THONG.docx
CHUAN_BI_BAO_VE_THAM_SO_THUAT_TOAN.txt
# .streamlit/secrets.toml chứa API key thật (OPENWEATHER_KEY, TOMTOM_KEY, ...) - KHÔNG được commit.
# Trước đây bỏ qua CẢ THƯ MỤC .streamlit/ để chắc chắn không lọt key, nhưng như vậy vô tình chặn luôn
# .streamlit/config.toml (cấu hình server KHÔNG chứa secret, cần được commit để áp dụng trên production
# qua deploy từ git) - giờ chỉ ignore đúng secrets.toml, không ignore cả thư mục.
.streamlit/secrets.toml
__pycache__/
*.pyc
